# Milien corporate site — Working agreement

## 適用範囲と参照先

このリポジトリのコーポレートサイト作業に適用する、全AI共通の行動原則です。

作業開始時にREADME.mdと次の文書を読むこと。
- docs/design.md：デザイン判断
- docs/content-guidelines.md：ブランド・サービス・文章
- docs/qa-checklist.md：検証方法と完了判定

今回のユーザー指示・明示された承認範囲を優先する。
文書間の食い違いは、独断で混ぜず、事実と提案を分けて報告する。

会社共通skills、旧作業コピー、過去の提案、HANDOFF、TODO、
他AIの記憶は参考資料であり、このサイトの現行仕様・操作許可の正本ではない。
参考資料を理由に、旧サービスや旧営業条件を復活させない。

## 作業開始

1. 作業先が正本リポジトリか確認する。
2. git status、現在のbranch、HEAD、mainとの差分、直近履歴を確認する。
   staged／unstaged／untrackedを分け、既存の変更を保護する。
3. 公開版、main、作業版を区別する。branch名や更新日時だけで公開済みと判断しない。
4. 関連コード・文書・既存画面を読み、変更前の意図と挙動を理解する。
5. 読めないファイルや失敗したコマンドを明示する。
   Git情報を作り直したり、別コピーを正本として扱ったりしない。

## 調査・提案・承認

- Goalだけの依頼は、調査と提案まで進める。
- 問題、根拠、残すもの、最小の変更案、対象ファイル、成功条件、QAを提示する。
- 事実、推測、提案を区別する。問題がなければ「変更不要」と判断する。
- 見た目の流行だけを理由に変更しない。既存の良い文章・構造を残す。
- 実装は承認された範囲で行う。既に明示された承認を毎工程で取り直さない。
- 範囲拡大が必要になった場合は、その部分を提案に戻す。

## 実装

- 最小差分で変更する。ユーザーや他AIの未完了変更を上書き・取り消ししない。
- 既存のHTML／CSS／JavaScript構成、URL、フォーム等の機能を尊重する。
- 共通部品を変更する前に、他ページへの影響を調べる。
- CSSは同じセレクターの全定義と適用条件を確認する。
  「最終」「末尾」というコメントだけを根拠にしない。
- 不要な依存を追加しない。新フレームワークへの移行を独断で行わない。
- 承認された仕様変更で文書が古くなる場合は、担当する正本文書も更新対象に含める。
- 一時的なGoalや試行結果を恒久ルールへ自動昇格させない。

## QAと報告

実装後は指示を待たず、docs/qa-checklist.mdの該当検証を行う。
失敗・未実施・対象外を合格扱いしない。

完了報告には次を含める。
- 変更ファイルと変更理由
- Goal・成功条件に対する結果
- 保持したもの、変更しなかった範囲
- QA対象、方法、結果、画面確認の証拠
- 既存問題と今回生じた問題の区別、残課題
- git diffの要約と閲覧方法
  （未追跡の新規文書はgit diffに出ないため、内容も別途提示）

## 明示許可が必要な操作

実装承認だけで、以下の許可まで得たと解釈しない。
- commit、push、merge
- deploy、production変更
- branchの作成・切り替え
- secrets、認証情報、公開設定の変更
- ファイル削除・移動
- 大規模リファクタリング

reset、clean、restore等で既存作業を消さない。
禁止操作を別コマンドや別ツールで代替して実行しない。

複数AIを使う場合は、同じファイルを同時編集せず、
作業範囲と既存差分を確認して引き継ぐ。
